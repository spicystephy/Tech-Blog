{{!-- {{#each posts as |post|}}
  <div class="row mb-4 project">
    <div class="col-md-5">
      <h3>
        <a href="/post/{{id}}">{{title}}</a>
      </h3>

    </div>
    <div class="col-md-7">
      <p>
        {{post_text}}
      </p>
      <p>
        Created by
        {{user.username}}
        on
        {{format_date post.date_created}}
      </p>
    </div>
  </div>
{{/each}} --}}